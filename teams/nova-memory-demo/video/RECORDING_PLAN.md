# Five-minute walkthrough capture plan

Record real session output after the two runs in `demo/README.md`. Do not stage a successful response or hide a failed retrieval. Avoid showing the TiDB password, DSN, API key, browser account details, or any real customer data.

| Time | Screen and narration |
| --- | --- |
| 0:00–0:35 | State the fictional Nova Marketplace overnight problem. Show identical starter workspaces and passing starter tests. |
| 0:35–1:15 | Show session-one decision to both agents. Show the baseline saying memory is unavailable and mem9 returning a memory ID in the other run. |
| 1:15–1:35 | Close both sessions, restart mem9, and inspect the saved memory ID. |
| 1:35–3:25 | Place the two fresh day-two sessions side by side. Show the same prompt and each agent's first recall result. Let each run proceed without correction. |
| 3:25–4:20 | Compare the resulting code and test output. Identify which details match the facilitator's original rule. |
| 4:20–5:00 | Show mem9's stored record and the findings page. State what worked and what did not. |

Capture the terminal at readable text size. Keep both runs visible during the comparison. Save an MP4 in this directory or add a viewing link to `video/README.md`. The final artifact should be about five minutes.
